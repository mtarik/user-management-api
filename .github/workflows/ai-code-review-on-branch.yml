name: 🤖 AI Code Review - Nouvelle Branche

on:
  # Se déclenche sur les Pull Requests
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '**.java'

  # Se déclenche à la création d'une branche
  create:

  # Se déclenche sur tout push (y compris première fois sur nouvelle branche)
  push:
    branches:
      - '**'  # Toutes les branches
    paths:
      - '**.java'

  # Permet le déclenchement manuel
  workflow_dispatch:

jobs:
  ai-code-review:
    # Ne s'exécute que si c'est une branche (pas un tag) ou une PR
    if: github.ref_type == 'branch' || github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📦 Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 🔍 Identifier les fichiers Java modifiés
        id: changed-files
        run: |
          # Gérer différemment selon le type d'événement
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            # Pour une PR, comparer avec la branche de base
            git diff --name-only origin/${{ github.base_ref }}...${{ github.sha }} | grep '\.java$' > changed_files.txt || true
          else
            # Pour un push ou création de branche, comparer avec main
            BASE_BRANCH="main"
            if git show-ref --verify --quiet refs/remotes/origin/$BASE_BRANCH; then
              git diff --name-only origin/$BASE_BRANCH...${{ github.sha }} | grep '\.java$' > changed_files.txt || true
            else
              # Si main n'existe pas, prendre tous les fichiers Java
              find . -name "*.java" -not -path "*/target/*" -not -path "*/.git/*" > changed_files.txt || true
            fi
          fi

          if [ -s changed_files.txt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
            echo "📝 Fichiers Java à analyser :"
            cat changed_files.txt
          else
            echo "found=false" >> $GITHUB_OUTPUT
            echo "❌ Aucun fichier Java à analyser"
          fi

      - name: ☕ Compilation du projet Java
        if: steps.changed-files.outputs.found == 'true'
        run: |
          if [ -f "pom.xml" ]; then
            echo "🔨 Compilation avec Maven..."
            mvn clean compile -B -q || echo "⚠️ Avertissement: Erreur de compilation"
          elif [ -f "build.gradle" ]; then
            echo "🔨 Compilation avec Gradle..."
            ./gradlew compileJava -q || echo "⚠️ Avertissement: Erreur de compilation"
          else
            echo "ℹ️ Aucun système de build détecté"
          fi
        continue-on-error: true

      - name: 🔍 Analyse statique (Checkstyle, PMD)
        if: steps.changed-files.outputs.found == 'true'
        run: |
          if [ -f "pom.xml" ]; then
            echo "📋 Exécution de Checkstyle..."
            mvn checkstyle:check || echo "⚠️ Problèmes Checkstyle détectés"
            echo "🔍 Exécution de PMD..."
            mvn pmd:check || echo "⚠️ Problèmes PMD détectés"
          fi
        continue-on-error: true

      - name: 🐍 Setup Python
        if: steps.changed-files.outputs.found == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Installer les dépendances Python
        if: steps.changed-files.outputs.found == 'true'
        run: |
          pip install -r scripts/requirements.txt

      - name: 🤖 Analyse IA avec Claude
        if: steps.changed-files.outputs.found == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_NAME: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          echo "🧠 Lancement de l'analyse IA..."
          echo "📍 Branche: ${{ github.ref_name }}"
          python scripts/ai_code_reviewer.py

      - name: 📊 Upload du rapport d'analyse
        if: always() && steps.changed-files.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ai-review-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            code_review_*.md
            code_review_*.json
            review_report.json
            review_report.md
          retention-days: 30

      - name: 🔍 Trouver la Pull Request associée
        if: steps.changed-files.outputs.found == 'true'
        id: find-pr
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // Si c'est un événement pull_request, on a directement le numéro
            if (context.eventName === 'pull_request') {
              core.setOutput('pr-number', context.issue.number);
              core.setOutput('has-pr', 'true');
              console.log(`✅ PR #${context.issue.number}`);
              return;
            }

            // Sinon, chercher une PR ouverte pour cette branche
            const branch = context.ref.replace('refs/heads/', '');
            const { data: pulls } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${branch}`,
              state: 'open'
            });

            if (pulls.length > 0) {
              core.setOutput('pr-number', pulls[0].number);
              core.setOutput('has-pr', 'true');
              console.log(`✅ PR trouvée: #${pulls[0].number}`);
            } else {
              core.setOutput('has-pr', 'false');
              console.log('ℹ️ Aucune PR ouverte pour cette branche');
            }

      - name: 💬 Créer un Review avec commentaires sur les lignes
        if: steps.changed-files.outputs.found == 'true' && steps.find-pr.outputs.has-pr == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            // Lire le rapport JSON
            const files = fs.readdirSync('.');
            const jsonFile = files.find(f => f.startsWith('code_review_') && f.endsWith('.json'));

            if (!jsonFile || !fs.existsSync(jsonFile)) {
              console.log('⚠️ Aucun rapport JSON trouvé');
              return;
            }

            const reviewData = JSON.parse(fs.readFileSync(jsonFile, 'utf8'));
            const comments = [];
            let summaryBody = '## 🤖 Revue de Code IA - Résumé\n\n';

            // Icônes pour les sévérités
            const severityIcons = {
              'critical': '🔴',
              'high': '🟠',
              'medium': '🟡',
              'low': '🔵',
              'info': 'ℹ️'
            };

            // Construire les commentaires sur les lignes
            for (const fileReview of reviewData.files || []) {
              const filePath = fileReview.path;
              summaryBody += `\n### 📄 ${filePath}\n`;
              summaryBody += `**Score**: ${fileReview.score || 'N/A'}/10\n\n`;

              let fileHasIssues = false;

              for (const issue of fileReview.issues || []) {
                if (issue.line) {
                  fileHasIssues = true;
                  const icon = severityIcons[issue.severity] || '•';
                  const commentBody = `${icon} **${issue.severity?.toUpperCase() || 'INFO'}** - ${issue.title || 'Problème détecté'}\n\n${issue.description || ''}\n\n${issue.suggestion ? `💡 **Suggestion**: ${issue.suggestion}` : ''}`;

                  comments.push({
                    path: filePath,
                    line: issue.line,
                    side: 'RIGHT',
                    body: commentBody
                  });
                }
              }

              if (!fileHasIssues && fileReview.issues?.length === 0) {
                summaryBody += '✅ Aucun problème détecté\n\n';
              }
            }

            // Ajouter le résumé global
            if (reviewData.summary) {
              summaryBody += `\n## 📊 Résumé Global\n\n${reviewData.summary}\n\n`;
            }
            if (reviewData.overall_score) {
              summaryBody += `**Score Global**: ${reviewData.overall_score}/100\n\n`;
            }

            summaryBody += '\n---\n';
            summaryBody += `🔍 Commit: \`${context.sha.substring(0, 7)}\`\n`;
            summaryBody += `⏰ Analysé le: ${new Date().toLocaleString('fr-FR')}\n`;
            summaryBody += `🤖 Propulsé par Claude Sonnet 4.5`;

            // Créer le review avec commentaires
            console.log(`📝 Création du review avec ${comments.length} commentaires`);

            try {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: ${{ steps.find-pr.outputs.pr-number }},
                event: 'COMMENT',
                body: summaryBody,
                comments: comments
              });
              console.log('✅ Review créé avec succès');
            } catch (error) {
              console.log('⚠️ Erreur lors de la création du review avec commentaires:', error.message);
              console.log('📝 Posting du résumé uniquement en commentaire');

              // Si les commentaires inline échouent, poster uniquement le résumé
              await github.rest.issues.createComment({
                issue_number: ${{ steps.find-pr.outputs.pr-number }},
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: summaryBody
              });
            }

      - name: ✅ Résumé de l'analyse
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 RÉSUMÉ DE L'ANALYSE IA"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🌿 Branche: ${{ github.ref_name }}"
          echo "📝 Commit: ${{ github.sha }}"

          if [ "${{ steps.changed-files.outputs.found }}" == "true" ]; then
            if ls code_review_*.json 1> /dev/null 2>&1; then
              echo "✅ Revue de code terminée avec succès"
              echo "📁 Rapport disponible dans les artefacts"
            else
              echo "⚠️ Aucun rapport généré"
            fi
          else
            echo "ℹ️ Aucun fichier Java à analyser"
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
