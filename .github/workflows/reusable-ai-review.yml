name: 🤖 Reusable AI Code Review - Java

on:
  workflow_call:
    inputs:
      java-version:
        description: 'Version de Java à utiliser'
        required: false
        type: string
        default: '17'
      build-tool:
        description: 'Outil de build (maven, gradle, ou auto)'
        required: false
        type: string
        default: 'auto'
      enable-static-analysis:
        description: 'Activer Checkstyle, PMD, SpotBugs'
        required: false
        type: boolean
        default: true
      post-pr-comment:
        description: 'Poster un commentaire sur la PR'
        required: false
        type: boolean
        default: true
      fail-on-critical:
        description: 'Échouer le workflow si problèmes critiques'
        required: false
        type: boolean
        default: false
    secrets:
      ANTHROPIC_API_KEY:
        description: 'Clé API Anthropic pour Claude'
        required: true

jobs:
  ai-code-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ inputs.java-version }}
          distribution: 'temurin'

      - name: 📦 Cache Maven
        if: inputs.build-tool == 'maven' || inputs.build-tool == 'auto'
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 📦 Cache Gradle
        if: inputs.build-tool == 'gradle' || inputs.build-tool == 'auto'
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: 🔍 Identifier les fichiers Java modifiés
        id: changed-files
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            git diff --name-only origin/${{ github.base_ref }}...${{ github.sha }} | grep '\.java$' > changed_files.txt || true
          else
            git diff --name-only HEAD~1 HEAD | grep '\.java$' > changed_files.txt || true
          fi

          if [ -s changed_files.txt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
            echo "📝 Fichiers Java modifiés :"
            cat changed_files.txt
          else
            echo "found=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Aucun fichier Java modifié"
          fi

      - name: ☕ Compilation du projet Java
        if: steps.changed-files.outputs.found == 'true'
        run: |
          BUILD_TOOL="${{ inputs.build-tool }}"

          # Auto-détection
          if [ "$BUILD_TOOL" == "auto" ]; then
            if [ -f "pom.xml" ]; then
              BUILD_TOOL="maven"
            elif [ -f "build.gradle" ] || [ -f "build.gradle.kts" ]; then
              BUILD_TOOL="gradle"
            fi
          fi

          # Compilation
          if [ "$BUILD_TOOL" == "maven" ]; then
            echo "🔨 Compilation avec Maven..."
            mvn clean compile -B -q || echo "⚠️ Avertissement: Erreur de compilation"
          elif [ "$BUILD_TOOL" == "gradle" ]; then
            echo "🔨 Compilation avec Gradle..."
            chmod +x gradlew || true
            ./gradlew compileJava -q || echo "⚠️ Avertissement: Erreur de compilation"
          else
            echo "ℹ️ Aucun système de build détecté"
          fi
        continue-on-error: true

      - name: 🔍 Analyse statique
        if: steps.changed-files.outputs.found == 'true' && inputs.enable-static-analysis
        run: |
          if [ -f "pom.xml" ]; then
            echo "📋 Exécution de Checkstyle..."
            mvn checkstyle:check || echo "⚠️ Problèmes Checkstyle détectés"
            echo "🔍 Exécution de PMD..."
            mvn pmd:check || echo "⚠️ Problèmes PMD détectés"
          fi
        continue-on-error: true

      - name: 🐍 Setup Python
        if: steps.changed-files.outputs.found == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📥 Télécharger le script AI Reviewer
        if: steps.changed-files.outputs.found == 'true'
        run: |
          mkdir -p scripts
          curl -sSL https://raw.githubusercontent.com/${{ github.repository_owner }}/java-ai-code-review/main/scripts/ai_code_reviewer.py \
            -o scripts/ai_code_reviewer.py
          curl -sSL https://raw.githubusercontent.com/${{ github.repository_owner }}/java-ai-code-review/main/scripts/requirements.txt \
            -o scripts/requirements.txt
          chmod +x scripts/ai_code_reviewer.py

      - name: 📦 Installer les dépendances Python
        if: steps.changed-files.outputs.found == 'true'
        run: |
          pip install -r scripts/requirements.txt

      - name: 🤖 Analyse IA avec Claude
        if: steps.changed-files.outputs.found == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO_NAME: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          echo "🧠 Lancement de l'analyse IA..."
          python scripts/ai_code_reviewer.py

      - name: 📊 Upload du rapport
        if: always() && steps.changed-files.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ai-review-report-${{ github.sha }}
          path: |
            code_review_*.md
            code_review_*.json
          retention-days: 30

      - name: 💬 Commenter la Pull Request
        if: github.event_name == 'pull_request' && steps.changed-files.outputs.found == 'true' && inputs.post-pr-comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            let reportContent = '## 🤖 Revue de Code IA - Résultats\n\n';

            const files = fs.readdirSync('.');
            const mdFile = files.find(f => f.startsWith('code_review_') && f.endsWith('.md'));

            if (mdFile && fs.existsSync(mdFile)) {
              reportContent += fs.readFileSync(mdFile, 'utf8');
            } else {
              reportContent += '✅ Analyse terminée. Aucun problème majeur détecté.';
            }

            reportContent += '\n\n---\n';
            reportContent += `🔍 Commit: \`${context.sha.substring(0, 7)}\`\n`;
            reportContent += `⏰ Analysé le: ${new Date().toLocaleString('fr-FR')}\n`;
            reportContent += `🤖 Propulsé par Claude Sonnet 4.5`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: reportContent
            });

      - name: 🔍 Vérifier les problèmes critiques
        if: steps.changed-files.outputs.found == 'true' && inputs.fail-on-critical
        run: |
          if [ -f "code_review_"*.json ]; then
            CRITICAL_COUNT=$(python3 -c "
            import json
            import glob
            files = glob.glob('code_review_*.json')
            if files:
                with open(files[0]) as f:
                    data = json.load(f)
                    count = sum(len([i for i in f.get('issues', []) if i.get('severity') == 'critical']) for f in data.get('files', []))
                    print(count)
            else:
                print(0)
            ")

            if [ "$CRITICAL_COUNT" -gt 0 ]; then
              echo "❌ $CRITICAL_COUNT problème(s) critique(s) détecté(s)!"
              exit 1
            fi
          fi

      - name: ✅ Résumé
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 RÉSUMÉ DE L'ANALYSE IA"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ "${{ steps.changed-files.outputs.found }}" == "true" ]; then
            if ls code_review_*.json 1> /dev/null 2>&1; then
              echo "✅ Revue de code terminée avec succès"
              echo "📁 Rapport disponible dans les artefacts"
            else
              echo "⚠️ Aucun rapport généré"
            fi
          else
            echo "ℹ️ Aucun fichier Java à analyser"
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
