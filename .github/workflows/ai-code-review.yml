# ════════════════════════════════════════════════════════════════════════════
# 📝 Workflow de Revue de Code IA v3.0 - Version Simplifiée
# ════════════════════════════════════════════════════════════════════════════
#
# ✨ Fonctionnalités v3.0 :
# - 📊 Scoring détaillé par catégorie (/10 avec quality labels)
# - 🔒 Analyse OWASP Top 10 2021 structurée
# - 🏗️ Analyse architecturale approfondie (patterns, violations, couplage)
# - ⚡ Optimisations performance détaillées (N+1, caching, complexité)
# - 🏷️ Auto-labelling automatique basé sur l'analyse
# - 💬 Commentaires inline sur Files Changed (comme une vraie code review)
# - 🚨 Système de gates (fail-on-critical) pour bloquer les merges
#
# 🎯 Architecture:
#   Toute l'intelligence est centralisée dans java-ai-code-review
#   Ce workflow ne fait qu'appeler l'agent mutualisé
#
# ════════════════════════════════════════════════════════════════════════════

name: 🤖 AI Code Review v3.0

on:
  # Se déclenche sur les Pull Requests
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '**.java'

  # Se déclenche sur les commits sur ces branches
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'feat/**'
      - 'hotfix/**'
    paths:
      - '**.java'

  # Permet le déclenchement manuel
  workflow_dispatch:

jobs:
  ai-code-review:
    name: 📊 Revue de Code IA v3.0
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      # ========================================================================
      # 1. Checkout et Setup
      # ========================================================================

      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📦 Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # ========================================================================
      # 2. Identifier les fichiers modifiés
      # ========================================================================

      - name: 🔍 Identifier les fichiers Java modifiés
        id: changed-files
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            git diff --name-only origin/${{ github.base_ref }}...${{ github.sha }} | grep '\.java$' > changed_files.txt || true
          else
            git diff --name-only HEAD~1 HEAD | grep '\.java$' > changed_files.txt || true
          fi

          if [ -s changed_files.txt ]; then
            echo "found=true" >> $GITHUB_OUTPUT
            echo "📝 Fichiers Java modifiés :"
            cat changed_files.txt
          else
            echo "found=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Aucun fichier Java modifié"
          fi

      # ========================================================================
      # 3. Compilation optionnelle (pour détecter les erreurs de build)
      # ========================================================================

      - name: ☕ Compilation du projet Java
        if: steps.changed-files.outputs.found == 'true'
        run: |
          if [ -f "pom.xml" ]; then
            echo "🔨 Compilation avec Maven..."
            mvn clean compile -B -q || echo "⚠️ Avertissement: Erreur de compilation"
          elif [ -f "build.gradle" ]; then
            echo "🔨 Compilation avec Gradle..."
            ./gradlew compileJava -q || echo "⚠️ Avertissement: Erreur de compilation"
          else
            echo "ℹ️ Aucun système de build détecté"
          fi
        continue-on-error: true

      # ========================================================================
      # 4. Setup Python et Agent IA
      # ========================================================================

      - name: 🐍 Setup Python
        if: steps.changed-files.outputs.found == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📥 Checkout AI Code Review Agent (Centralisé)
        if: steps.changed-files.outputs.found == 'true'
        uses: actions/checkout@v4
        with:
          repository: mtarik/java-ai-code-review
          path: .ai-review-agent
          token: ${{ secrets.PAT_GITHUB_TOKEN }}

      - name: 📦 Installer les dépendances de l'agent
        if: steps.changed-files.outputs.found == 'true'
        run: |
          pip install -r .ai-review-agent/scripts/requirements.txt

      # ========================================================================
      # 5. Exécuter l'analyse IA complète (Analyse + Posting + Labels)
      # ========================================================================

      - name: 🤖 Revue de Code IA Complète (Claude Sonnet 4.5)
        if: steps.changed-files.outputs.found == 'true'
        env:
          # API Keys
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # Configuration GitHub
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO_NAME: ${{ github.repository }}
          GITHUB_SHA: ${{ github.sha }}

          # Configuration IA
          AI_MODEL: "claude-sonnet-4-5"

          # Options
          FAIL_ON_CRITICAL: "false"
          CHANGED_FILES: "changed_files.txt"
        run: |
          echo "🚀 Lancement de l'agent IA Code Review v3.0..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          # Exécuter le script principal
          # Ce script fait TOUT:
          # - Analyse le code avec l'IA
          # - Génère les rapports JSON et Markdown
          # - Poste les commentaires inline sur la PR
          # - Ajoute les labels automatiques
          python .ai-review-agent/scripts/review_and_post.py

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Revue de code IA terminée"

      # ========================================================================
      # 6. Sauvegarder les rapports comme artifacts
      # ========================================================================

      - name: 📤 Upload des rapports
        if: steps.changed-files.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ai-code-review-reports
          path: |
            code_review_*.json
            code_review_*.md
            review_report.md
          retention-days: 30

      # ========================================================================
      # 7. Résumé du workflow
      # ========================================================================

      - name: 📊 Résumé
        if: always() && steps.changed-files.outputs.found == 'true'
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Résumé du Workflow AI Code Review"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "✅ Fichiers analysés: $(wc -l < changed_files.txt 2>/dev/null || echo 0)"
          echo "✅ Rapports générés et sauvegardés dans les artifacts"
          echo "✅ Commentaires inline postés sur la PR (si applicable)"
          echo "✅ Labels automatiques ajoutés (si applicable)"
          echo ""
          echo "🤖 Propulsé par Claude Sonnet 4.5 - AI Code Review v3.0"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
