# ════════════════════════════════════════════════════════════════════════════
# 📝 Workflow de Revue de Code IA - Appelle le workflow centralisé
# ════════════════════════════════════════════════════════════════════════════

name: 🤖 AI Code Review

on:
  # Se déclenche sur les Pull Requests
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '**.java'

  # Se déclenche sur les commits sur ces branches
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'feat/**'
      - 'hotfix/**'
    paths:
      - '**.java'

  # Permet le déclenchement manuel
  workflow_dispatch:

jobs:
  test-coverage:
    name: 🧪 Tests & Couverture
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📦 Cache Maven
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: 🧪 Exécuter les tests avec couverture
        run: |
          echo "🧪 Exécution des tests unitaires et d'intégration..."
          mvn clean test jacoco:report -B
        continue-on-error: false

      - name: 📊 Analyser les résultats des tests
        if: always()
        run: |
          # Extraire les statistiques des tests
          if [ -f target/surefire-reports/TEST-*.xml ]; then
            TOTAL_TESTS=$(grep -r "tests=" target/surefire-reports/TEST-*.xml | sed 's/.*tests="\([0-9]*\)".*/\1/' | awk '{s+=$1} END {print s}')
            FAILURES=$(grep -r "failures=" target/surefire-reports/TEST-*.xml | sed 's/.*failures="\([0-9]*\)".*/\1/' | awk '{s+=$1} END {print s}')
            ERRORS=$(grep -r "errors=" target/surefire-reports/TEST-*.xml | sed 's/.*errors="\([0-9]*\)".*/\1/' | awk '{s+=$1} END {print s}')
            SUCCESS=$((TOTAL_TESTS - FAILURES - ERRORS))

            # Extraire la couverture
            COVERAGE="N/A"
            if [ -f "target/site/jacoco/index.html" ]; then
              COVERAGE=$(grep -A 1 "Total" target/site/jacoco/index.html | grep -oP '\d+%' | head -1 || echo "N/A")
            fi

            # Créer badge de status
            if [ $FAILURES -eq 0 ] && [ $ERRORS -eq 0 ]; then
              STATUS="✅ $SUCCESS/$TOTAL_TESTS tests passed | 📊 Coverage: $COVERAGE"
            else
              STATUS="❌ $((FAILURES + ERRORS)) tests failed | $SUCCESS/$TOTAL_TESTS passed"
            fi

            echo "$STATUS" > test-summary.txt
            echo "$STATUS"
          else
            echo "⚠️ No test results found" > test-summary.txt
          fi

      - name: 📊 Upload rapport de couverture
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-${{ github.sha }}
          path: |
            target/site/jacoco/
            target/surefire-reports/
          retention-days: 30

      - name: 💬 Poster résumé des tests
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            let summary = '⚠️ Tests non exécutés';
            if (fs.existsSync('test-summary.txt')) {
              summary = fs.readFileSync('test-summary.txt', 'utf8').trim();
            }

            const reportContent = `### 🧪 Tests & Couverture\n\n${summary}\n\n_📁 Rapport détaillé dans les artefacts • 🤖 Revue de code dans "Files changed"_`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: reportContent
            });

  call-ai-review:
    name: 📊 Analyse IA du code Java
    needs: test-coverage
    permissions:
      contents: read
      pull-requests: write
      issues: write
    uses: mtarik/java-ai-code-review/.github/workflows/reusable-ai-review.yml@main

    with:
      # Version de Java (17, 11, 8, etc.)
      java-version: '17'

      # Outil de build: 'auto', 'maven', ou 'gradle'
      build-tool: 'auto'

      # Activer les analyses statiques (Checkstyle, PMD)
      enable-static-analysis: true

      # Poster un commentaire sur la Pull Request
      post-pr-comment: true

      # Échouer le workflow si des problèmes critiques sont détectés
      fail-on-critical: false

    secrets:
      # La clé API Anthropic doit être configurée dans les secrets du repository
      # Settings → Secrets and variables → Actions → New repository secret
      # Nom: ANTHROPIC_API_KEY
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
